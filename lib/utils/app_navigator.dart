import 'package:flutter/material.dart';

/// Global navigator key for navigation outside of BuildContext (e.g. after account deletion).
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();
